_base_: csl_daily_signtrajfield_v3_sentence_memory_phase_a_motion_contrast_v1.yaml

# One optimizer-step integration smoke only. This overlay deliberately disables
# terminal scientific feasibility so a tiny, statistically meaningless sample
# can exercise checkpointing and all four validation modes without promotion.
experiment_name: csl_daily_signtrajfield_v3_sentence_memory_phase_a_motion_contrast_v1_smoke

train:
  epochs: 1
  early_stopping_patience: 0
  early_stopping_min_epochs: 1

selection:
  require_feasible: false

output:
  out_dir: experiments/NIAF/continuous_trajectory_field/csl_daily_signtrajfield_v3_sentence_memory_phase_a_motion_contrast_v1_smoke
